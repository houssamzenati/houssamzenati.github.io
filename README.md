# Houssam Zenati Website

Personal academic website built with Jekyll and hosted on GitHub Pages.

## Local preview

```bash
bundle install
bundle exec jekyll serve
```

Then open `http://localhost:4000`.
